// SPDX-License-Identifier: UNLICENSED
// compiler version must be greater than or equal to 0.8.3 and less than 0.9.0
pragma solidity ^0.8.9;
import "./Dimension.sol";

struct OmniverseDimensions {
    Dimension EighthDimension;
    Dimension NinthDimension;
    Dimension TenthDimension;
    Dimension EleventhDimension;
    Dimension TwelfthDimension;
    Dimension[] CustomDimensions;
}